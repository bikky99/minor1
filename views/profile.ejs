<%- include('./include/header') %>
<div class="">
<div class="container py-md-5 container ">
  <%- include('include/flash')%>
  <%- include('include/profileShared')%>

 
  <div class="list-group">
    <% posts.forEach(function(post) { %>
    <a
      href="/post/<%= post._id %>"
      class="list-group-item list-group-item-action littleDark"
    >
      <img class="avatar-tiny" src="<%= post.author.avatar %>" />
      <strong><%= post.title %></strong>
      <h6 class="pt-2"><%=post.body%></h6>
      <span class="text-muted small"
        >on <%= post.createdDate.getMonth() +1%>/<%= post.createdDate.getDate()
        %>/<%= post.createdDate.getFullYear() %></span
      >
    </a>
    <% }) %>
  </div>
</div>
</div>

<%- include('./include/footer') %>
